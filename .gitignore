node_modules/
.env.local
.env*.local
.next/
out/
.DS_Store
*.tsbuildinfo
.idea/

# Compiled blueprint JSON, regenerated by `pnpm convert:blueprints`
# (and automatically before `pnpm dev` / `pnpm build`). Served as static
# assets from /blueprints/<slug>.json so the kit can fetch fresh copies
# in dev without needing HMR for JSON modules.
web/public/blueprints/

# Local telemetry written by the Claude Code hooks. See README.md "Telemetry".
.leapter/telemetry.jsonl
.leapter/telemetry.sent

# TypeScript build output for workspace packages
packages/*/dist/
